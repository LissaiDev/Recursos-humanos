import { Text, SafeAreaView, View, StatusBar } from "react-native";
const Options = ()=>{
    return(
        <View>
            <Text>
                Options
            </Text>
        </View>
    )
}

export default Options;